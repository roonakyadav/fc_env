
# Gradio 4.44.0 + huggingface_hub: HfFolder import; avoid hub>=0.20
huggingface_hub==0.19.4
# Gradio 4.44.0 Jinja/Starlette template path breaks on FastAPI 0.115+ / Starlette 1.x (GET /ui/ → 500).
# Pin a compatible pair so /ui/ renders (see server/app.py mount at /ui/).
fastapi==0.111.0
uvicorn[standard]>=0.27.0
pydantic>=2.0.0
gradio==4.44.0
gymnasium>=0.29.0
numpy>=1.26.0

# Training (lazy-imported in gradio / train)
matplotlib>=3.9.0
stable-baselines3>=2.2.0
tensorboard>=2.10.0
torch>=2.0.0

# TRL PPO (use TRL <1.0; 1.x removed PPOTrainer from main API)
trl>=0.7.0,<1.0
transformers>=4.35.0
accelerate>=0.20.0
